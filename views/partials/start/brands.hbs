<div class="center" style="margin: 10px 0">
    <div class="row center">
        {{#each brands}}
            <a class="col s3 m6 l3" href="/catalog/product/{{id}}/brand">
                <div class="flexCenter card-panel hoverable brandsMy" style="background:var(--zircon);">
                    <img src="{{photoURL}}" alt="">
                </div>
            </a>
        {{/each}}
    </div>
</div>